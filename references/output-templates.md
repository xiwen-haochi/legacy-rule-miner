# Output Templates

Templates for the single-file `.rules.md` and module-specific `.rules-{module}.md` files.
Use these as the skeleton during Step 4.

---

## Main File: `.rules.md`

```markdown
# {Project Name} — AI Coding Rules

> Auto-generated by legacy-rule-miner. Provides project constraints for AI coding assistants.
> The `## Custom Rules` section is user-maintained and will not be overwritten during updates.

## Project Overview

### Tech Stack
- **Language**: {detected_language} {detected_language_version}
- **Framework**: {detected_framework} {detected_framework_version}
- **Build Tool**: {detected_build_tool}
- **Runtime**: {detected_runtime_version}

### Key Dependencies
| Dependency | Version | Purpose |
|-----------|---------|---------|
| ... | ... | ... |

### Module Structure
[Describe module structure — monolith / multi-module, what each module does]

### Build & Run
- **Build**: `...`
- **Test**: `...`
- **Run**: `...`

### Environment Variables
| Variable | Purpose | Example |
|----------|---------|---------|
| ... | ... | ... |

### Important Context
[Critical context the AI must know before modifying any code]

---

## Architecture

### Project Structure
```
[Simplified directory tree showing the layer structure]
```

### Layer Rules
#### [Layer name, e.g., Controller Layer]
**MUST**: [Where controllers live, what they can/cannot do]

#### [Layer name, e.g., Service Layer]
**MUST**: [Where services live, what they can/cannot do]

#### [Layer name, e.g., Data Access Layer]
**MUST**: [Where DAOs/repositories live, query patterns]

### Dependency Direction
**MUST**: Dependencies flow [e.g., Controller → Service → Repository]
**NEVER**: [e.g., Repository must never import from Controller or Service packages]

### Where to Put New Code
#### Adding a new API endpoint
1. Controller: `[path pattern]`
2. Service: `[path pattern]`
3. DAO/Repository: `[path pattern]`
4. DTO/VO: `[path pattern]`

### Prohibited Patterns
**NEVER**: This project does not use the following patterns — do not introduce them:
- [e.g., Do not introduce event sourcing]
- [e.g., Do not create a service locator]

---

## Naming Conventions

### Classes
| Type | Pattern | Examples |
|------|---------|---------|
| Controller | `[pattern]` | `UserController`, `OrderController` |
| Service | `[pattern]` | `UserService`, `OrderService` |
| Service Impl | `[pattern]` | `UserServiceImpl` |
| DAO/Repository | `[pattern]` | `UserMapper`, `UserRepository` |
| Entity/Model | `[pattern]` | `User`, `OrderItem` |
| DTO | `[pattern]` | `CreateUserDTO`, `UserVO` |
| Exception | `[pattern]` | `BusinessException` |
| Constant | `[pattern]` | `CommonConstants`, `ErrorCode` |
| Util | `[pattern]` | `DateUtils`, `StringHelper` |

### Methods
| Action | Pattern | Examples |
|--------|---------|---------|
| Get single | `[e.g., getXxx]` | `getUserById` |
| Get list | `[e.g., listXxx]` | `listUsersByDept` |
| Create | `[e.g., createXxx]` | `createUser` |
| Update | `[e.g., updateXxx]` | `updateUserStatus` |
| Delete | `[e.g., deleteXxx]` | `deleteUser` |
| Check | `[e.g., isXxx]` | `checkPermission` |

### Variables
- **MUST**: [e.g., Constants use UPPER_SNAKE_CASE]
- **SHOULD**: [e.g., Boolean variable prefix: is/has/can/should]

### Files & Packages
- **MUST**: [Package/directory naming pattern]
- **SHOULD**: [File naming pattern]

### Database
| Entity | Pattern | Examples |
|--------|---------|---------|
| Table | `[e.g., snake_case singular]` | `user`, `order_item` |
| Column | `[e.g., snake_case]` | `user_name`, `created_at` |
| Primary Key | `[e.g., id]` | `id` |
| Foreign Key | `[e.g., xxx_id]` | `user_id` |

### API URL
- **MUST**: [URL pattern, e.g., /api/v1/{resource}]

---

## Coding Patterns

### Error Handling
**MUST**: [How exceptions are thrown and caught in this project]

**DO:**
```{lang}
[Real code from the project]
```

**DON'T:**
```{lang}
[What AI might write instead]
```

### Logging
- **ERROR**: [When to use]
- **WARN**: [When to use]
- **INFO**: [When to use]

**SHOULD**: [Project logging convention]

### Input Validation
**MUST**: [How inputs are validated in this project]

### Return Values
**MUST**: [How service methods return values]
**MUST**: [How controllers wrap responses]

### Import Conventions
**MUST**: [This project's import style/ordering/alias convention]

**DO:**
```{lang}
[Project's standard import style]
```

**DON'T:**
```{lang}
[Do not use inconsistent aliases or different import styles]
```

### Null Handling
**SHOULD**: [How the project handles null values]

### Date/Time Handling
**SHOULD**: [Date/time library and patterns used]

### Constant Definitions
**MUST**: [Where and how constants are defined — avoid duplication]
- [e.g., All error codes defined in ErrorCode class, must not be redefined in other files]

---

## API Design

### URL Patterns
| Action | HTTP Method | URL Pattern | Example |
|--------|------------|-------------|---------|
| List | [GET/POST] | [pattern] | [example] |
| Detail | [GET] | [pattern] | [example] |
| Create | [POST] | [pattern] | [example] |
| Update | [PUT/POST] | [pattern] | [example] |
| Delete | [DELETE/POST] | [pattern] | [example] |

### Request Format
**MUST**: [How request parameters are passed]

### Response Format
**MUST**: All endpoints return this standard envelope:
```json
[Real response example from the project]
```

### Error Codes
| Code | Meaning |
|------|---------|
| ... | ... |

### Pagination
**MUST**: [Pagination parameter convention]

### Authentication
**MUST**: [How authentication is applied to endpoints]

---

## Data Access

### ORM Framework
[e.g., MyBatis / Hibernate / Eloquent / SQLAlchemy]

### Query Patterns
**MUST**: [How queries are written]

**DO:**
```{lang}
[Real query code from the project]
```

**DON'T:**
```{lang}
[What AI might write instead]
```

### Transaction Management
**MUST**: [How transactions are managed]

### Entity Conventions
**MUST**: [Entity/Model class requirements]
- [e.g., All entities extend BaseEntity]
- [e.g., Audit fields: created_at, updated_at auto-populated]
- [e.g., Soft delete via is_deleted column]

### Database Change Strategy
**MUST**: [Field change principles]
- [e.g., Add > Modify: prefer adding new columns + deprecating old ones over altering existing column types/names]
- [e.g., Migration file naming convention]

### Cross-Table Field Dependencies
**MUST**: [Related table update constraints]
- [e.g., When modifying user.name, must also update order.buyer_name]
- [List all known field dependency relationships]

### Common Query Patterns
[Document 2-3 typical query patterns used in the project]

---

## Security

### Authentication
**MUST**: [Auth mechanism and how to apply it]

### Authorization
**MUST**: [How permissions are checked]

### Input Validation
**MUST**: [Validation approach]

### Sensitive Data
**NEVER**: [Rules about sensitive data in logs, responses, etc.]

### Known Security Considerations
[Project-specific security notes]

---

## Dependencies

### Key Dependencies (Version-Locked)
| Dependency | Version | Notes |
|-----------|---------|-------|
| ... | ... | ... |

### Introducing New Dependencies
**MUST**: Before adding a new dependency:
1. Check if an existing dependency already provides the functionality
2. Verify version compatibility with {framework} {version}
3. [Other project-specific requirements]

**NEVER**: Do not upgrade existing dependency versions without explicit approval

### Known Compatibility Constraints
- [e.g., Spring Boot 2.1.x requires Spring Framework 5.1.x — do not mix]

### Preferred Libraries
| Need | Use This | Not This |
|------|----------|----------|
| [e.g., JSON] | [e.g., FastJSON] | [e.g., Gson / Jackson] |

---

## Known Pitfalls

### Critical (Production-Breaking)

#### [Pitfall Title]
**Location**: [file path or module]
**What**: [Description of the trap]
**What happens if violated**: [Consequence]
**Correct approach**: [What to do instead]

### Important (Bug-Causing)

#### [Pitfall Title]
...

### Caution (Style/Convention)

#### [Pitfall Title]
...

### Historical Workarounds
These workarounds exist for a reason. Do not "fix" them.

#### [Workaround Title]
**Location**: [file path]
**What it does**: [Description]
**Why it exists**: [History]
**NEVER**: Do not remove or refactor this code without fully understanding the context

### Do-Not-Touch Zones
These files/modules should not be modified without extreme caution:
- `[path]` — [reason]

---

## Custom Rules

<!-- This section is user-maintained. legacy-rule-miner will not overwrite it during updates. -->
<!-- Add custom rules, team conventions, or supplements/overrides to auto-generated rules here. -->

```

### Minimum Requirements Checklist

Before finalizing `.rules.md`, verify:

- [ ] **Project Overview**: Language + version, Framework + version, at least one build command, ≥5 key dependencies with versions
- [ ] **Architecture**: Layer pattern identified, dependency direction stated, "where to put new code" with path patterns, ≥2 prohibited patterns
- [ ] **Naming Conventions**: ≥3 class types with naming patterns, method naming for CRUD, DB naming, real project examples
- [ ] **Coding Patterns**: Error handling with DO/DON'T code, logging convention, import/alias conventions, constant definition rules, ≥3 patterns with real code
- [ ] **API Design**: URL pattern with examples, response envelope format, ≥3 real endpoint examples
- [ ] **Data Access**: ORM identified, query pattern with code example, transaction rule, field change strategy, cross-table dependencies (if any)
- [ ] **Security**: Auth mechanism described, input validation approach
- [ ] **Dependencies**: Top 10 dependencies with versions, new-dep introduction policy
- [ ] **Known Pitfalls**: All items from comment archaeology (TODO/FIXME/HACK), all items from user interview, each pitfall has location + consequence
- [ ] **Custom Rules**: Empty placeholder section present

---

## Module-Specific File: `.rules-{module}.md`

```markdown
# {Module Name} — Module-Level AI Coding Rules

> This file supplements the main rule file `.rules.md`. All main rules remain in effect unless explicitly overridden below.

## Module Overview
[What this module does, its importance, risk level]

## Overrides
[Rules that differ from the global rules]

## Additional Rules

### [Rule Title]
**MUST/SHOULD/NEVER**: [Module-specific rule]

## Module-Specific Pitfalls
[Pitfalls unique to this module]

## Custom Rules

<!-- This section is user-maintained and preserved during updates. -->

```

### Module File Minimum Requirements
- [ ] Module overview explaining its purpose
- [ ] At least one rule that differs from global rules (otherwise this file shouldn't exist)
- [ ] Custom Rules placeholder section present

---

## Incremental Update Guide

When `.rules.md` already exists:

1. **Read the existing file** — Parse sections by `## ` headers
2. **Identify user-maintained sections** — `## Custom Rules` and any `<!-- custom -->` marked blocks
3. **Update auto-generated sections** — Replace content of `## Project Overview` through `## Known Pitfalls`
4. **Preserve user sections** — Copy `## Custom Rules` content verbatim into the updated file
5. **Show diff summary** — Tell the user what changed (new rules added, modified rules, removed rules)

When `.rules-{module}.md` already exists:
- Same approach: update auto-generated content, preserve `## Custom Rules`
- If the module no longer needs special rules, ask the user before deleting the file
